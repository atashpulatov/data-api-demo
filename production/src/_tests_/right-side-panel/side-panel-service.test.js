import React from 'react';
import sidePanelService from '../../right-side-panel/side-panel-service';

describe('SidePanelService', () => {
  it.skip('should dispatch duplicateRequested for duplicate with import', () => {
    // TODO: AZ
    expect(true).toBeFalse();
  });

  it.skip('should dispatch popupActions.callForDuplicate for duplicate with edit', () => {
    // TODO: AZ
    expect(true).toBeFalse();
  });

  it.skip('should call setDuplicatedObjectId and setSidePanelPopup with given parameters for setDuplicatePopup', () => {
    // TODO: AZ
    expect(true).toBeFalse();
  });
});
